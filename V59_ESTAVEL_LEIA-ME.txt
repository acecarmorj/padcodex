# V59 estável — fallback JSONP preventivo

Versão gerada após a correção do erro "API indisponível" no painel.

## Regras mantidas

- index.html: tablet, funciona offline depois do primeiro preparo online.
- lab.html: tablet, online.
- super.html: tablet, online.
- painel.html: computador, online.
- 007.html: computador, online.
- secretaria.html: computador, online.

## Correções aplicadas nesta v59

1. Mantida a URL oficial da API:
   https://script.google.com/macros/s/AKfycbyevPoSgnnlyJXhaphkPCjEvFBMq1gdqTbrZ521JdXukkq2_PUPjOGuG0gmEAoZjYeX6w/exec

2. Mantido o fallback JSONP do painel que corrigiu o erro:
   "API indisponível. Painel sem dados válidos para o recorte."

3. Adicionado fallback JSONP preventivo nos carregamentos seguros dos módulos online:
   - painel / coordenação
   - laboratório
   - supervisão
   - secretaria
   - 007 / trilha de agentes
   - cadastros: apenas listagem de agentes
   - ferramentas de privacidade/status

4. Login e gravações continuam por POST:
   - login não usa fallback JSONP para não colocar senha na URL;
   - salvar resultado de laboratório continua por POST;
   - salvar documentos da secretaria continua por POST;
   - salvar/excluir usuários continua por POST;
   - publicar/limpar plano LIRAa continua por POST.

5. API atualizada para permitir JSONP/GET somente em leituras autenticadas e status:
   - panel_bundle_private
   - panel_session_status
   - panel_session_refresh
   - secretaria_bundle_private
   - admin_agents_list
   - panel_007_bundle já existia

6. sw-acs.js atualizado para cache v59, mantendo runtime-config.js em network-first.

7. Pasta .git removida do pacote final.

## Arquivos de API dentro do ZIP

- api.txt
- API_ATUALIZADA_V59.txt
- backend/Code.gs

Use backend/Code.gs ou api.txt para substituir o Code.gs no Google Apps Script.
