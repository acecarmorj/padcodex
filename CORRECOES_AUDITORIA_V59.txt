AUDITORIA E CORREÇÕES V59 - 2026-05-18

URL oficial da API configurada:
https://script.google.com/macros/s/AKfycbyevPoSgnnlyJXhaphkPCjEvFBMq1gdqTbrZ521JdXukkq2_PUPjOGuG0gmEAoZjYeX6w/exec

Regras preservadas:
- index.html é offline após o primeiro preparo online.
- lab.html, super.html, painel.html, 007.html e secretaria.html continuam online.

Correções aplicadas:
1. Pasta .git removida do pacote final.
2. runtime-config.js continua disponível offline para o index, mas agora é network-first para evitar API velha em cache.
3. Sessão operacional do campo agora usa Script Properties + CacheService; o cache continua otimizando, mas a validade real pode chegar a 24h.
4. Trilha de localização do 007 protegida: sessão de agente não pode forjar matrícula/nome de outro agente.
5. Função de forçar ADM inicial bloqueada por padrão. Para emergência, exige ACE_ALLOW_DEFAULT_ADMIN_FORCE=true em Script Properties e remove a autorização após uso.
6. Cadastro administrativo de usuários agora valida CPF com dígitos verificadores, com exceção controlada apenas para a conta ADM inicial de emergência.
7. Chamadas autenticadas principais do index e do painel passaram a usar POST com token/sessão no corpo, reduzindo exposição em URL.

Arquivos principais alterados:
- api.txt
- API_ATUALIZADA_V59.txt
- backend/Code.gs
- sw-acs.js
- assets/runtime-config.js
- assets/index-actions.js
- assets/painel-app.js

Validação local:
- Sintaxe JavaScript verificada com node --check nos arquivos alterados.
- A API foi verificada como JavaScript válido ao copiar api.txt para arquivo .js temporário.
- Não foi feita implantação real no Google Apps Script; após copiar backend/Code.gs ou api.txt, publique uma nova versão do Web App.
